# Dudas y puntos para repasar

- Como se comporta `pid: host` en Docker Desktop frente a Docker en Linux nativo.
- Que permisos concretos puede necesitar `/proc/<pid>/fd` cuando el proceso pertenece a otro usuario.
- Como cambia la interpretacion de `policy` si aparece `SCHED_DEADLINE` en kernels nuevos.
